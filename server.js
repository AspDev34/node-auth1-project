const express = require('express'); //imports express library for use
const session = require('express-session'); //imports express-session library for use
const knexStore = require('connect-session-knex') (session); 